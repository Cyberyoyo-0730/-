/*#include<iostream>
#include<iomanip>
using namespace std;
int main() {
	int a, b, c;
	cin >> a >> b >> c;
	cout<< left << setw(8) << a
		<< left << setw(8) << b
		<< left << setw(8) << c;
	return 0;
}*/
/*若输入超出八个字符宽度的数字则直接显示完整数字（如123456789）；
  若输入负数则“-”也算一个字符宽度（如-1）
*/