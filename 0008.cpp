/*#include <iostream>
using namespace std;

int main() {
    char ch;
    cin >> ch;
    // 控制等腰三角形的 3 行
    for (int i = 0; i < 3; ++i) {
        // 打印每行前面的空格，空格数逐渐减少
        for (int j = 0; j < 2 - i; ++j) {
            cout << " ";
        }
        // 打印每行的字符，字符数逐渐增加（奇数个）
        for (int k = 0; k < 2 * i + 1; ++k) {
            cout << ch;
        }
        cout << endl;
    }
    return 0;
}*/
